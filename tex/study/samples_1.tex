% Book, article or letter (also set font size, columns etc)
% book - with chapters, article - with indents etc.
\documentclass[12pt]{letter}

% unclude additional packages (complex formules, several pages tables)
\usepackage{amsfonts,longtable}


% set text parameters here

\parindent=0cm % no indents

% Text starts here
\begin{document}

% Create center environment
\begin{center}

% Title
\Huge{\LaTeXe\ examples} \\\

% End center environment
\end{center}


% start group LARGE font
\LARGE{

% show special symbols
This is special symbols: \\
\{, \}, \$, \&, \#, \%, \_, \",\^ \\ 

% show different fonts
{\slshape Italic\label{fonts} font} \\
{\bfseries Bold font} \\
{{\slshape \bfseries Bold italic font} } \\

}

% \large is differs from LARGE (and from Large)
\large{
% this text links with label {fonts} below
% (use \ref instead of \pageref to show chapter instead of page)
See labels usage:~\pageref{fonts} \\
}

% Formules includes in $$ symbols
\large{
$a$, $b$ - cathetuses \\
$c$ - hypotenuse \\\\
Pythagoras theorem: \\
$a^2 + b^2 = c^2$ \\

Upper index: $R^i$ \\
Lower index: $R_j$ \\
Both: $R^i_j$ \\
Several symbols index: $R^{ij}_{km}$ \\
Recursive: $x^{y^3}$
}

% start a new page
\newpage

% Fractions: (x/y), frac{}{}
\Large{
Fractions: \\
Unequality $2\le x+1/x$ is true for any $x$ \\
$x^2 < x^4$ for $x\ge1$ \\
$\pi\approx3,14$ \\
$\frac{x+2}{x^2-4} = \frac{1}{x-2}$
\\

% braces: \left( \right)
Braces: \\
$1 + \left( \frac{1}{x^3 + 1} \right)^3 \approx 2, x\to0$
\\

% roots
Roots: \\
$\sqrt[3]{x^3}=x$, but $\sqrt{x^2}=|x|$
\\

% Strokes (derivatives etc.)
Strokes: \\
$(fg)'' = f''g + 2f'g' + fg''$ (Leibniz formula) \\
\\ 

Dots: \\
$N = \{1,2,\cdots,100\}$ \\

Functions: \\
$\sin(\pi/6) = 1/2$ \\
$\log_{1/16}(2) = -1/4$ \\
$f(x) = e^{x^2}$
















}

\end{document}